# DV Project

## Instructions to run

- Installation
```
npm install
```

- Running the project
```
npm run dev
```

- Create a new component in the _components_ folder
- Import the component in _App.jsx_
- Pass the data as a prop to the component
- Take a look at _components/Example.jsx_